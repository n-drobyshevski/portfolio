const Technologie = ({ name }: { name: string }) => {
  return (
    <div className="rounded-full border border-neutral-500 px-4 py-1 text-sm">
      {name}
    </div>
  );
};

export default Technologie;
